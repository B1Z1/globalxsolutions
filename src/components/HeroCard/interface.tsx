import { IImageFixed } from '../../shared/interfaces/image-fixed'

interface IPropsHeroCard {
  name: string
  description: string
  photo: IImageFixed
  profession: string
}

export { IPropsHeroCard }
