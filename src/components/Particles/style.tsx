import styled from 'styled-components'

const StyleCanvas = styled.canvas`
  position: absolute;
  top: 0;
  left: 0;
  mix-blend-mode: color-burn;
  pointer-events: none;
  z-index: 100;
`

export { StyleCanvas }
