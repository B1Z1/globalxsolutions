import styled from 'styled-components'

const StyleCanvas = styled.canvas`
  position: absolute;
  top: 0;
  left: 0;
  pointer-events: none;
  mix-blend-mode: darken;
  z-index: 100;
`

export { StyleCanvas }
