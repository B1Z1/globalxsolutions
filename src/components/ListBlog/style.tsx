import styled from 'styled-components'

const StyleElement = styled.li``

const StyleTitle = styled.h3`
  display: inline-block;
  position: relative;
  margin-bottom: 8px;
`

const StyleContent = styled.p`
  margin-bottom: 0;
`

export { StyleElement, StyleTitle, StyleContent }
