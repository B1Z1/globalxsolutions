import styled from 'styled-components'

const StylePostWrapper = styled.div`
  display: flex;
  position: relative;
`

const StyleContentWrapper = styled.div`
  flex: 0 0 75%;
  position: relative;
  max-width: 75%;
  padding-right: 16px;
`

const StyleGalleryWrapper = styled.div`
  position: relative;
`

export { StylePostWrapper, StyleContentWrapper, StyleGalleryWrapper }
